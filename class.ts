class Person {
    first: string;
    last: string;
  }
  
  let person = new Person();
  person.first = 'Sagar';
  person.last = 'Chaudhari';
  
  console.log(person.first + ' ' + person.last);